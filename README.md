# adjacency_list_tree_editor
Full-stack tree editor built on ADJACENCY_LIST. Extends the base class with dynamic meta columns (auto-created via ALTER TABLE), includes a PHP/AJAX backend and a dark-themed ECharts visualizer with node selection, drag-to-zoom, inline rename, move mode and toast feedback.
